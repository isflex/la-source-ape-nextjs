# Fix Typescript compilation errors

Catched bugs & errors that must be fixed before proceeding with build stage or commiting any changes to git repository origin

## Instructions

1. Retrieve the latest typescript compilation logs from :

   - `tmp/typescript_errors_latest.txt`

2. Fix the errors
