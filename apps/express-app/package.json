{
  "name": "express-app",
  "version": "1.0.0",
  "description": "Express node server to run .next gateway standalone build",
  "license": "MIT",
  "author": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x bin/postbuild.sh && ./bin/postbuild.sh",
    "serve": "node dist/index.js",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "5.1.0"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "@types/node": "22.10.1",
    "ts-node": "10.9.2",
    "typescript": "5.7.2"
  }
}
