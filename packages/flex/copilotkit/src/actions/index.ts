/**
 * @flexiness/copilotkit/actions
 *
 * Action creation utilities for CopilotKit
 */

export type { ActionParameter, ActionDefinition, ActionRenderProps } from './types';
export { createAction, createActions, param, ActionTemplates } from './createAction';
